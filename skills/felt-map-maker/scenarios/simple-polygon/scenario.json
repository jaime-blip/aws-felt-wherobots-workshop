{
  "prompt": "Style these regions as simple polygons",
  "output_type": "FSL",
  "criteria": [
    "Must include version '2.3.1'",
    "Must use type 'simple'",
    "Must include paint.color property (never omit it)",
    "Color must be from SimplePalette (HSL format like 'hsl(104,33%,57%)') or a reasonable single color - NOT an array of colors",
    "Must NOT include paint.size property (polygons don't have size - this is a common mistake)",
    "Must use default strokeWidth: 1 (if included)",
    "Must use default opacity: 0.8",
    "Must use strokeColor: 'auto'"
  ]
}
