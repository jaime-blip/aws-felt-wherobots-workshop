[project]
name = "skill-evals"
version = "0.1.0"
description = "Evaluation framework for agent skills"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.40.0",
    "rich>=13.0.0",
    "junitparser>=3.0.0",
]
